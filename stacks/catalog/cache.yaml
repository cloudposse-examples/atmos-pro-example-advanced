components:
  terraform:
    cache:
      dependencies:
        components:
          - component: vpc
      vars:
        vpc_id: !terraform.state vpc ".vpc_id // ""mock-vpc-id"""
        cache_version: "1.0.0"
