components:
  terraform:
    load-balancer:
      dependencies:
        components:
          - component: vpc
      vars:
        vpc_id: !terraform.state vpc ".vpc_id // ""mock-vpc-id"""
        lb_version: "1.0.0"
