components:
  terraform:
    cluster:
      settings:
        depends_on:
          1:
            component: vpc
          2:
            component: load-balancer
      vars:
        vpc_id: !terraform.state vpc vpc_id
        lb_id: !terraform.state load-balancer lb_id
