components:
  terraform:
    cdn:
      settings:
        depends_on:
          1:
            component: object-storage
          2:
            component: frontend
      vars:
        storage_id: !terraform.state object-storage storage_id
        frontend_id: !terraform.state frontend frontend_id
