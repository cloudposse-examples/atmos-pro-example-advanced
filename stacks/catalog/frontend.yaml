components:
  terraform:
    frontend:
      dependencies:
        components:
          - component: api
          - component: cache
      vars:
        api_id: !terraform.state api ".api_id // ""mock-api-id"""
        cache_id: !terraform.state cache ".cache_id // ""mock-cache-id"""
