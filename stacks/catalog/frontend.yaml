components:
  terraform:
    frontend:
      settings:
        depends_on:
          1:
            component: api
          2:
            component: cache
      vars:
        api_id: !terraform.state api api_id
        cache_id: !terraform.state cache cache_id
