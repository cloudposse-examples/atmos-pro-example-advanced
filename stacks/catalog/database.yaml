components:
  terraform:
    database:
      dependencies:
        components:
          - component: vpc
      vars:
        vpc_id: !terraform.state vpc ".vpc_id // ""mock-vpc-id"""
        database_version: "1.0.0"
