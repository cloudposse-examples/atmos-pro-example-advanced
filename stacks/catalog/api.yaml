components:
  terraform:
    api:
      settings:
        depends_on:
          1:
            component: database
          2:
            component: cluster
      vars:
        database_id: !terraform.state database database_id
        cluster_id: !terraform.state cluster cluster_id
